# Copy this file to .env.local and fill in your Finnhub API key.
# Sign up free at https://finnhub.io/register — no credit card needed.
#
# If set here, this key takes priority over anything saved via the UI.
# The app works fine without this — you can paste the key in the UI instead.

NEXT_PUBLIC_FINNHUB_KEY=
